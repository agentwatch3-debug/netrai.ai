"use client";
import { FormEvent, useState } from "react";
import { Button } from "@/components/ui/button";
export function ApiKeySettings() { const [name, setName] = useState(""); const [newKey, setNewKey] = useState<string | null>(null); const [keyId, setKeyId] = useState(""); async function create(event: FormEvent) { event.preventDefault(); const response = await fetch("/api/api-keys", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ name }) }); if (response.ok) setNewKey((await response.json()).key); } async function revoke() { await fetch(`/api/api-keys/${keyId}`, { method: "DELETE" }); setKeyId(""); } return <div className="max-w-xl space-y-5"><form className="flex gap-2" onSubmit={create}><input className="h-9 flex-1 rounded-md border bg-slate-950 px-3 text-sm" placeholder="Key name" value={name} onChange={(event) => setName(event.target.value)}/><Button>Create key</Button></form>{newKey && <div className="rounded-md border border-amber-700 bg-amber-950/30 p-3 text-sm"><p className="mb-2 text-amber-300">Copy this key now. It will not be shown again.</p><code className="break-all">{newKey}</code></div>}<div className="flex gap-2 border-t pt-5"><input className="h-9 flex-1 rounded-md border bg-slate-950 px-3 text-sm" placeholder="API key ID to revoke" value={keyId} onChange={(event) => setKeyId(event.target.value)}/><Button className="bg-red-600 hover:bg-red-500" type="button" onClick={() => void revoke()}>Revoke</Button></div></div>; }
