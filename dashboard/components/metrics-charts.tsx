"use client";
import { Area, AreaChart, Bar, BarChart, CartesianGrid, Line, LineChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
import { Card } from "@/components/ui/card";
import { AnalyticsPoint } from "@/lib/types";
const charts = [{ key: "cost_usd", title: "Daily cost", kind: "area", format: (v: number) => `$${v.toFixed(2)}` }, { key: "prompt_tokens", title: "Daily token usage", kind: "bar", format: (v: number) => v.toLocaleString() }, { key: "error_rate", title: "Error rate", kind: "line", format: (v: number) => `${(v * 100).toFixed(1)}%` }, { key: "p95_latency_ms", title: "p50 / p95 latency", kind: "line", format: (v: number) => `${v} ms` }];
export function MetricsCharts({ data }: { data: AnalyticsPoint[] }) { return <div className="grid gap-4 lg:grid-cols-2">{charts.map(({ key, title, kind, format }) => <Card className="h-72 p-4" key={key}><h2 className="mb-4 text-sm font-medium">{title}</h2><ResponsiveContainer width="100%" height="88%">{kind === "area" ? <AreaChart data={data}><CartesianGrid strokeDasharray="3 3"/><XAxis dataKey="date"/><YAxis/><Tooltip formatter={format}/><Area dataKey={key} type="monotone" stroke="#60a5fa" fill="#1d4ed8" fillOpacity={0.25}/></AreaChart> : kind === "bar" ? <BarChart data={data}><CartesianGrid strokeDasharray="3 3"/><XAxis dataKey="date"/><YAxis/><Tooltip formatter={format}/><Bar dataKey="prompt_tokens" stackId="a" fill="#60a5fa"/><Bar dataKey="completion_tokens" stackId="a" fill="#818cf8"/></BarChart> : <LineChart data={data}><CartesianGrid strokeDasharray="3 3"/><XAxis dataKey="date"/><YAxis/><Tooltip formatter={format}/><Line dataKey={key} type="monotone" stroke="#60a5fa" dot={false}/>{key === "p95_latency_ms" && <Line dataKey="p50_latency_ms" type="monotone" stroke="#a78bfa" dot={false}/>}</LineChart>}</ResponsiveContainer></Card>)}</div>; }
